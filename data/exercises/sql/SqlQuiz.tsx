import React from 'react';
import { PlaceholderContent } from '../../components';

const SqlQuiz: React.FC = () => {
    return (
        <div>
            <PlaceholderContent title="SQL Quiz" />
        </div>
    );
};

export default SqlQuiz;

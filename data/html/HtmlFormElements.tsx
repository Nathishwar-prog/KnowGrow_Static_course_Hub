import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlFormElements: React.FC = () => (
  <PlaceholderContent title="HTML Form Elements" />
);

export default HtmlFormElements;

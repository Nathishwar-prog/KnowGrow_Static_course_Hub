import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlAccessibility: React.FC = () => (
  <PlaceholderContent title="HTML Accessibility" />
);

export default HtmlAccessibility;

import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlHttpMethods: React.FC = () => (
  <PlaceholderContent title="HTTP Methods" />
);

export default HtmlHttpMethods;

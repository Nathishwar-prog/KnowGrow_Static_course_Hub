import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlInterviewPrep: React.FC = () => (
  <PlaceholderContent title="HTML Interview Prep" />
);

export default HtmlInterviewPrep;

import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlAttributesRef: React.FC = () => (
  <PlaceholderContent title="HTML Attributes Reference" />
);

export default HtmlAttributesRef;

import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlWebWorkers: React.FC = () => (
  <PlaceholderContent title="HTML Web Workers" />
);

export default HtmlWebWorkers;

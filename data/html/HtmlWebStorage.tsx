import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlWebStorage: React.FC = () => (
  <PlaceholderContent title="HTML Web Storage" />
);

export default HtmlWebStorage;

import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlHttpMessages: React.FC = () => (
  <PlaceholderContent title="HTTP Messages" />
);

export default HtmlHttpMessages;

import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlColorsRef: React.FC = () => (
  <PlaceholderContent title="HTML Colors Reference" />
);

export default HtmlColorsRef;

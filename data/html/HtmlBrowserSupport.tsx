import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlBrowserSupport: React.FC = () => (
  <PlaceholderContent title="HTML Browser Support" />
);

export default HtmlBrowserSupport;

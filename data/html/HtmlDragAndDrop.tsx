import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlDragAndDrop: React.FC = () => (
  <PlaceholderContent title="HTML Drag and Drop" />
);

export default HtmlDragAndDrop;

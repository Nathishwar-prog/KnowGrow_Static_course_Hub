import React from 'react';
import { PlaceholderContent } from '../components';

const HtmlUrlEncodeRef: React.FC = () => (
  <PlaceholderContent title="HTML URL Encode Reference" />
);

export default HtmlUrlEncodeRef;

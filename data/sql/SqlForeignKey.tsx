import React from 'react';
import { PlaceholderContent } from '../components';

const SqlForeignKey: React.FC = () => (
  <PlaceholderContent title="SQL Foreign Key" />
);

export default SqlForeignKey;
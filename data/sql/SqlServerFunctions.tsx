import React from 'react';
import { PlaceholderContent } from '../components';

const SqlServerFunctions: React.FC = () => (
  <PlaceholderContent title="SQL Server Functions" />
);

export default SqlServerFunctions;
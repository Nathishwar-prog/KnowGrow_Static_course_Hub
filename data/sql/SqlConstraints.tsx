import React from 'react';
import { PlaceholderContent } from '../components';

const SqlConstraints: React.FC = () => (
  <PlaceholderContent title="SQL Constraints" />
);

export default SqlConstraints;
import React from 'react';
import { PlaceholderContent } from '../components';

const SqlStoredProcedures: React.FC = () => (
  <PlaceholderContent title="SQL Stored Procedures" />
);

export default SqlStoredProcedures;
import React from 'react';
import { PlaceholderContent } from '../components';

const JsonStringify: React.FC = () => (
  <PlaceholderContent title="JSON Stringify" />
);

export default JsonStringify;
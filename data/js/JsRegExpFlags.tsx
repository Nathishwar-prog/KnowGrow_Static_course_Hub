import React from 'react';
import { PlaceholderContent } from '../components';

const JsRegExpFlags: React.FC = () => (
  <PlaceholderContent title="JS RegExp Flags" />
);

export default JsRegExpFlags;
import React from 'react';
import { PlaceholderContent } from '../components';

const DomCollections: React.FC = () => (
  <PlaceholderContent title="DOM Collections" />
);

export default DomCollections;
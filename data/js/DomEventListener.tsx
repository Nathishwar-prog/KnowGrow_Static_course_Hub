import React from 'react';
import { PlaceholderContent } from '../components';

const DomEventListener: React.FC = () => (
  <PlaceholderContent title="DOM Event Listener" />
);

export default DomEventListener;
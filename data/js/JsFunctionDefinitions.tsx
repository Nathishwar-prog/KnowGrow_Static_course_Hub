import React from 'react';
import { PlaceholderContent } from '../components';

const JsFunctionDefinitions: React.FC = () => (
  <PlaceholderContent title="Function Definitions" />
);

export default JsFunctionDefinitions;